USES DOS,CRT;
VAR F:TEXT;
	 S:STRING;
	 R:REAL;
	 A,B,C:INTEGER;
BEGIN
	ASSIGN(F,'JUMP.TXT');
	REWRITE(F);
	FOR A:=1 TO 180 DO
	BEGIN
		B:=100-ROUND(100*SIN(A/57.3));
		STR(B,S);
		WRITE(B,' ');
		IF A MOD 20 <> 0 THEN WRITE(F,S+',')
							  ELSE WRITELN(F,S);
	END;
	CLOSE(F);
END.

